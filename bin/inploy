#!/usr/bin/env ruby

require 'rubygems'
require 'inploy'

deploy = Inploy::Deploy.new
deploy.configure

if ARGV.size > 1
  deploy.remote_install :from => ARGV[1].sub('from=', '')
else
  deploy.send "remote_#{ARGV[0]}"
end
